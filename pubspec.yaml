name: nomna_locale
description: Nomna Locale
version: 1.2.0
repository: https://github.com/zitherharpnomna/locale

environment:
  sdk: 3.9.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  generate: true
  uses-material-design: true
